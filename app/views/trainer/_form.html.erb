# form for creating a new trainer profile

<h1> Sign Up Here! </h1>

<%= form_for @trainer do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <br>
    <%= f.label :email %>
    <%= f.text_field :email %>
    <br>
    <%= f.label :password %>
    <%= f.password_field :password %>
    <br> 
    <%= f.label :bio %>
    <%= f.tex_area :bio %>
    <br>

    <%= f.submit %>

    <% end %>