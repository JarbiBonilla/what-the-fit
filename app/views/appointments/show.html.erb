# will show all the appointments that a specific trainer has created for his clients.


<%= @appointment.appointment_time.strftime("%A %B %e, %Y %l:%M %p") %>
<%= @appointment.paid_for %>
$<%= @appointment.price %>


<%= button_to "Edit Appointment", edit_appointment_path(@appointment), method: :get %>
<br>

<%= button_to "Delete Appointment", appointment_path(@appointment), method: :delete %>