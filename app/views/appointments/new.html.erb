# will render from _form.html.erb to create a new appointment for a client.
<%= render partial: "layouts/errors", locals: {item: @appointment} %>

<h1> Add a new appointment <%= @client.name %>! </h1>


<%= render 'form' %>